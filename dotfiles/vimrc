" SOURCE: https://vim.wikia.com/wiki/Example_vimrc

" 基础设置
set nocompatible            " 关闭兼容模式
set hidden                  " 允许切换未保存的 buffer
set confirm                 " 操作前弹出确认对话
set wildmenu                " 命令行补全增强
set showcmd                 " 显示部分输入命令
set cmdheight=2             " 命令行高度设置

" 文件类型和语法高亮
if has('syntax')
  syntax on
endif
if has('filetype')
  filetype plugin indent on
endif

" 搜索行为
set ignorecase              " 默认不区分大小写
set smartcase               " 有大写字母时强制区分
set hlsearch                " 搜索结果高亮

" 显示设置
set number                  " 显示行号
set ruler                   " 显示光标位置
set laststatus=2            " 始终显示状态栏
set visualbell              " 关闭响铃，改为闪烁
set t_vb=
set notimeout
set ttimeout
set ttimeoutlen=200

" 缩进设置（使用空格）
set autoindent
set shiftwidth=2
set softtabstop=2
set expandtab

" 如需使用真实 Tab，替换以上三行为：
" set noexpandtab
" set shiftwidth=4
" set tabstop=4

" 编辑行为
set backspace=indent,eol,start
set nostartofline

" 鼠标支持（如可用则开启）
if has('mouse')
  set mouse=a
endif

" 快捷键映射
nnoremap Y y$                       " Y 复制到行尾
nnoremap <C-L> :nohlsearch<CR><C-L> " 清除搜索高亮并刷新屏幕

" 可选功能
" set modeline                     " 若启用，请使用 securemodelines 插件
" set paste                        " 粘贴模式手动启用，可映射 <F11> 切换
" set pastetoggle=<F11>